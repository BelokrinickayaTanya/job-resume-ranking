# job-resume-ranking
Прототип алгоритма поиска и функции по оценке и проверке соответствия кандидатов

## 📋 ОГЛАВЛЕНИЕ
- [О проекте](#-о-проекте)
- [Ключевая инновация](#-ключевая-инновация)
- [Архитектура](#-архитектура)
- [Методы ранжирования](#-методы-ранжирования)
- [Установка и запуск](#-установка-и-запуск)


## 🎯 О ПРОЕКТЕ (на данный момент - прототип)

**Job-resume-ranking** — гибридная система двунаправленного ранжирования для автоматического сопоставления резюме и вакансий. Проект реализован на основе методов, описанных в статье *"Job Vacancy Ranking with Sentence Embeddings, Keywords, and Named Entities"* (Vanetik & Kogan, 2023).

### 🏆 КЛЮЧЕВАЯ ИННОВАЦИЯ

**Применение в matching резюме-вакансии двунаправленного ранжирования (Bidirectional Ranking)** — учет не только прямого соответствия вакансии кандидату, но и **позиции кандидата среди всех соискателей**.

Это имитирует реальную HR-практику: *«"Какой кандидат лучше для этой вакансии?" (ранжирование кандидатов)»*.

### 📊 БИЗНЕС-ЦЕННОСТЬ

Автоматизация рутинных задач HR, повышение точности отбора кандидатов.

---

## 🏗 АРХИТЕКТУРА

job-resume-ranking/

├── .venv/                          # Виртуальное окружение

├── .gitignore

├── README.md

├── requirements.txt

├── data/

│   ├── CV/                       # Папка с резюме (1-65.docx)

│   ├── vacancies/

│   │   └── 5_vacancies.csv      # 5 целевых вакансий

│   └── annotations/

│       └── ground_truth.py      # Аннотации из статьи

├── src/

│   ├── __init__.py

│   ├── pipeline/

│   │   ├── __init__.py

│   │   ├── document_loader.py   # Загрузка DOCX/CSV

│   │   ├── text_extractor.py    # Извлечение текста

│   │   └── data_saver.py        # Сохранение обработанных

│   ├── analysis/

│   │   ├── __init__.py

│   │   ├── language_detector.py

│   │   ├── keyword_extractor.py # KeyBERT

│   │   ├── named_entity.py      # SpaCy NER

│   │   ├── experience_calculator.py

│   │   ├── grammar_checker.py   # LanguageTool

│   │   └── error_detector.py

│   ├── matching/

│   │   ├── __init__.py

│   │   ├── keyword_matcher.py   # 60% веса

│   │   ├── tfidf_matcher.py     # 25% веса

│   │   ├── semantic_matcher.py  # 15% веса

│   │   └── unified_scorer.py


│   ├── ranking/

│   │   ├── __init__.py

│   │   ├── vm_method.py        # Vector Matching из статьи

│   │   └── ranker.py


│   └── evaluation/

│       ├── __init__.py

│       └── metrics.py          # Krippendorff, Spearman


├── notebooks/

│   └── evaluation.ipynb        # Визуализация результатов

│
├── tests/

│   ├── test_pipeline.py

│   └── test_matching.py

│
└── main.py                     # Точка входа

## 🧮 МЕТОДЫ РАНЖИРОВАНИЯ

### 1. **VM METHOD** (Vector Matching) — из статьи
```python
- Resume: Full text
- Vacancy: BERT extractive summary (10 sentences)
- Text representation: Character n-grams (1-3)
- Distance: L1 (Manhattan)
- Target Krippendorff's Alpha: 0.6287

⚙️ УСТАНОВКА И ЗАПУСК
🔧 Системные требования
Python 3.10+
8+ GB RAM (рекомендуется 16 GB)
5+ GB свободного места

# 1. Клонирование
git clone https://github.com/BelokrinickayaTanya/job-resume-ranking.git
cd job-resume-ranking

# 2. Виртуальное окружение
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
.venv\Scripts\activate     # Windows

# 3. Установка зависимостей
pip install --upgrade pip
pip install -r requirements.txt
pip install -e .

# 4. Загрузка моделей
python -m spacy download en_core_web_sm
python -m spacy download ru_core_news_sm
python -c "import nltk; nltk.download('punkt')"


# Тест на 1 резюме
python main.py --limit 1 --method unified --verbose

# Полный анализ на 30 резюме (двунаправленное ранжирование)
python main.py --limit 30 --method bidirectional --evaluate --report --fast

# Сравнение всех методов
python main.py --limit 30 --method all --evaluate --report --fast

# Оптимизация весов
python main.py --limit 30 --method ensemble --optimize --evaluate

Модуль оценки качества реализован в src/evaluation/metrics.py, который содержит классы RankingMetrics для расчета метрик и Evaluator для комплексной оценки методов ранжирования.

Проект содержит 8 автоматических тестов, которые проверяют критически важные компоненты системы:

**Модуль,	тест,	что проверяет,	почему это важно:**
KeywordMatcher,	test_exact_match,	точное совпадение навыков,	базовая функциональность матчинга;
test_synonym_match,	поиск по синонимам (reactjs → react),	учет различных формулировок в резюме;
UnifiedScorer,	test_score_calculation,	полный цикл расчета скора,	работоспособность основного алгоритма;
test_weights,	корректность весов (60/25/15),	фиксация настроек улучшенной версии;
DocumentLoader,	test_load_cv_docx,	чтение DOCX-файлов,	корректная загрузка резюме;
test_load_vacancies,	загрузка вакансий из CSV,	правильная работа с входными данными;
TextExtractor,	test_clean_text,	счистка и нормализация текста,	качество предобработки;
test_extract_experience_periods,	извлечение периодов опыта,	точность подсчета стажа.

Запуск всех тестов:
# Из корневой папки проекта
python -m pytest tests/ -v
